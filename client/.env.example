# ── Client environment variables ──────────────────────────────────────────────
# Leave VITE_API_URL empty → app runs fully on mock data (no server needed)
# Set VITE_API_URL → app talks to your real Express server
VITE_API_URL=
